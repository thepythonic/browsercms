<%= simple_form_for(model, as: :form_response, url: url) do |f| %>
    <%= hidden_field_tag :form_id, model.form.id %>
    <% model.form.fields.each do |field| %>
        <%= f.input field.name, label: field.label %>
    <% end %>
    <%= f.button :submit, "Submit" %>
<% end %>