<%= f.input name,
            label: label,
            wrapper: :append do
  h = f.input_field name, disabled: true, readonly: 'readonly'
  h << link_to("Edit", "#", data: {edit_path: edit_path}, class: "btn edit_form_button")
  h
end %>