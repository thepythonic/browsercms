<%= f.input field.name,
            label: field.label,
            wrapper: :append do
  h = f.input_field field.name, field.options(disabled: true)
  h << link_to("Edit", "#", data: {edit_path: edit_path}, class: "btn edit_form_button")
  h
end %>